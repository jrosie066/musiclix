import { searchArtistAction, searchReducer } from '../search-reducer';

describe('#searchReducer', () => {
  describe('#searchArtistAction', () => {
    it('should make a success call to api', () => {
      const actual;
    });
  });
});
